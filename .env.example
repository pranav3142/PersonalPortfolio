# Server-side only — read by the backend in api/chat.ts.
# This value must NEVER be prefixed with VITE_: Vite inlines any VITE_* variable
# into the browser bundle, which is exactly how the previous key was exposed.
GEMINI_API_KEY=your_gemini_api_key_here
