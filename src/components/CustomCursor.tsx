

/**
 * CustomCursor component that replaces the default browser cursor on desktop devices
 * Tracks mouse position and renders a styled cursor element with smooth follow animation
 * Only renders on desktop devices (min-width: 1024px)
 */
export function CustomCursor() {


  // Disabled for performance - custom cursor causes significant lag
  return null;
}
